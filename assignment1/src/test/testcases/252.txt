class Program {
            var x: int = 10;
            func @main():int {
                do {
                    self.x := self.x - 1;
                } while self.x > 0;
                return self.x;
            }
        }